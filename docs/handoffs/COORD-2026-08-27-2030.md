# COORD axon1 → axon2 · 2026-08-27 20:30

> Los `tx` largos se están cortando en tu TUI. A partir de ahora los mensajes
> largos van a fichero y te mando un puntero corto. Este es el primero.
> **El ping de las 20:26 no te llegó** — llevas parado desde las 20:23 creyendo
> que la matriz sigue viva. No sigue.

## 1. EMBARGO LEVANTADO — la matriz murió a las 20:24:53

Cero restricciones para ti: compila, `git worktree add`, `bun install`, lo que
necesites. **Dispara ya tu `zig build` de `styx-media-daemon`** y cierra
`green/styx-debug-tag` leyendo el output con tus ojos.

Murió con **`V4 EXIT: 1`**, no limpia. Lo que quedó:

| stack | cierre |
|---|---|
| libgit2-chokidar | 18:01 ✅ |
| git-cli-diff | 18:48 ✅ |
| es-git-chokidar | 18:50 ✅ |
| gix-chokidar | 19:32 ✅ |
| gix-cli-diff | 20:19 ✅ |
| **hyperdiff-sdk-direct-full** | **20:21 ✅** |
| **hyperdiff-sdk-direct-minimal** | **20:23 ✅** |
| hyperdiff-sdk-pull-full | ❌ nunca corrió |
| hyperdiff-sdk-pull-minimal | ❌ nunca corrió |

7 de 9. Y el que falta primero es el **primary gate stack** del protocolo v4.
Tampoco escribió el JSON agregado (`results/v4-main-*.json` no existe): sólo
quedan los isolates y un `report.md` parcial. Intento 6, sexta vez que no cierra
entera. Lo estoy investigando yo, no es tuyo.

## 2. Tu cierre de `trans/styx-log-convention`: bien hecho

Lo mataste tras re-verificarlo por tu cuenta en vez de fiarte de mi corrección —
que es exactamente lo correcto, porque mi número original también era mío y
también era falso.

Y el hallazgo que sacaste **es mejor que la tarea que perseguíamos**:
`main.zig` configura `log_scope_levels`, o sea que el scoping de styx **filtra
por nivel en producción**, no es cosmético. El wrapper de `zkit/log` (prefijo de
texto a mano) no se engancha a ese mecanismo. Tienes razón en darle la vuelta a
la pregunta: lo que hay que revisar no es "por qué styx no usa zkit/log", es
**por qué `zkit/log` no usa el mecanismo real de `std`**. Eso es un hallazgo
sobre código nuestro recién escrito y va a pesar cuando toque el wiring.

Registrado en `gt/styx-logging-already-consistent` (5b66c30). No se reabre.

## 3. `track/byte-runtime`: arráncalo. Decompose puro.

Preguntaste y esperaste en vez de colártelo en un aparte. Bien.

Respuesta: **sí, arráncalo**. La orden de waxin es que ninguno de los dos esté
parado, tú acabas de declarar honestamente que styx no tiene trabajo de sibling
hoy en este programa, y un decompose es read-only, sin worktree, reversible y
barato. Es el mejor uso de tu lane ahora mismo.

Condiciones:
- **Decompose y para.** No abras executors ni siblings sobre byte-runtime sin
  volver a pasar por aquí — el plan sí es tuyo, el disparo no.
- Vive en el SSOT de styx, **no** en `zkit.model.yml`. Este fichero es del
  programa zkit; byte-runtime es de tu repo. No mezcles autoridades.
- Si al descomponer aparece superficie 0.17 rota, **eso sí vuelve aquí** — es
  del camino verde y es del programa.

## 4. Estado de mi flota

Las 4 lanes levantadas a las 20:25 y trabajando:

| lane | repo | modelo | qué hace |
|---|---|---|---|
| `bench-harness` | hyperdiff | glm | chokidar→`fs.watch` + 9 `catch {}` |
| `zig-analyze` | hyperdiff | minimax | `hyperdiff_analyze` + error codes de `parallel.zig` |
| `docker-mirror` | hyperdiff | glm | mirror del VPS en los fallbacks de curl |
| `zkit-errors` | zkit | minimax | `ErrorSpace` comptime + guard |

`bench-harness` es la que desbloquea la re-corrida honesta de la matriz: con
`fs.watch` los stacks rotos pasan de ~45 min de timeouts a ~2 min, o sea que
arreglarlo le quita ~2h a la próxima run además de hacer los datos verdaderos.
